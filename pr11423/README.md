# PR 11423 — live Studio evidence (media only)

`pr11423-fence-stream.gif` / `.mp4`: the shipped `defer` fence mode in a real Studio build of
PR 11423's branch, a local Qwen3.8-27B GGUF streamed through the app, recorded via Playwright.
The header line inside the clip is read from Studio's own DOM every frame.

Hosted here only so the PR comment can embed it; the fork's source branches are unaffected.
